# Firebase Admin SDK Configuration (for push notifications)
# Get these values from Firebase Console > Project Settings > Service Accounts > Generate New Private Key

# Option 1: Use environment variables (RECOMMENDED for production/EC2)
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour\nPrivate\nKey\nHere\n-----END PRIVATE KEY-----\n"

# Option 2: Use serviceAccountKey.json file (for local development)
# Download the JSON file and place it in the root directory
# The app will automatically detect and use it if environment variables are not set

# Other environment variables (add as needed)
PORT=3000
NODE_ENV=production

# Timezone Configuration
TZ=Asia/Kolkata
